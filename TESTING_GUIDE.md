# 功能测试指南

## 测试步骤

### 1. 首页搜索功能测试

1. 启动开发服务器：
   ```bash
   npm run dev
   ```

2. 访问首页：http://localhost:3000

3. 测试搜索框：
   - [ ] 在搜索框输入关键词（如"户部巷"）
   - [ ] 点击"搜索"按钮
   - [ ] 验证是否跳转到 `/search?q=户部巷`
   - [ ] 验证搜索页面是否显示搜索结果

4. 测试回车键搜索：
   - [ ] 在搜索框输入关键词
   - [ ] 按回车键
   - [ ] 验证是否跳转到搜索页面

### 2. 夜市筛选功能测试

1. 访问夜市列表页：http://localhost:3000/markets

2. 测试评分筛选：
   - [ ] 点击"⭐ 4.5+"按钮
   - [ ] 验证按钮是否变为橙色（激活状态）
   - [ ] 验证 URL 是否包含 `?rating=4.5`
   - [ ] 验证列表是否只显示评分 ≥ 4.5 的夜市
   - [ ] 再次点击按钮取消筛选
   - [ ] 验证列表恢复显示所有夜市

3. 测试区域筛选：
   - [ ] 点击任意区域按钮（如"江岸区"）
   - [ ] 验证按钮是否变为紫色（激活状态）
   - [ ] 验证 URL 是否包含 `?district=江岸区`
   - [ ] 验证列表是否只显示该区域的夜市

4. 测试组合筛选：
   - [ ] 同时选择评分和区域筛选
   - [ ] 验证 URL 包含两个参数
   - [ ] 验证列表正确过滤

5. 测试清除筛选：
   - [ ] 选择任意筛选条件
   - [ ] 点击"✕ 清除筛选"按钮
   - [ ] 验证所有筛选条件被清除
   - [ ] 验证 URL 参数被移除
   - [ ] 验证列表显示所有夜市

### 3. 跑山筛选功能测试

1. 访问跑山列表页：http://localhost:3000/routes

2. 测试评分筛选：
   - [ ] 点击"⭐ 4.5+"按钮
   - [ ] 验证按钮是否变为绿色（激活状态）
   - [ ] 验证 URL 是否包含 `?rating=4.5`
   - [ ] 验证列表是否只显示评分 ≥ 4.5 的路线

3. 测试难度筛选：
   - [ ] 点击任意难度按钮（如"中等"）
   - [ ] 验证按钮是否变为蓝色（激活状态）
   - [ ] 验证 URL 是否包含 `?difficulty=中等`
   - [ ] 验证列表是否只显示该难度的路线

4. 测试组合筛选和清除功能（同夜市测试）

### 4. 搜索页面测试

1. 测试从首页跳转：
   - [ ] 从首页搜索"户部巷"
   - [ ] 验证跳转到 `/search?q=户部巷`
   - [ ] 验证搜索框已填充关键词
   - [ ] 验证自动执行搜索并显示结果

2. 测试直接访问带参数的 URL：
   - [ ] 直接访问 `http://localhost:3000/search?q=东湖`
   - [ ] 验证页面自动搜索并显示结果

## 预期结果

### 搜索功能
- ✅ 输入关键词后点击搜索按钮能正确跳转
- ✅ 输入关键词后按回车键能正确跳转
- ✅ 搜索页面能正确接收并显示搜索关键词
- ✅ 搜索页面能自动执行搜索

### 筛选功能
- ✅ 筛选按钮点击后有视觉反馈（颜色变化）
- ✅ 筛选后列表数据正确过滤
- ✅ 筛选状态同步到 URL 参数
- ✅ 可以同时应用多个筛选条件
- ✅ 清除筛选功能正常工作
- ✅ 刷新页面后筛选状态保持（通过 URL 参数）

### 视觉效果
- ✅ 保持赛博朋克风格
- ✅ 按钮激活状态明显
- ✅ 过渡动画流畅
- ✅ 响应式布局正常

## 已知限制

1. 如果数据库中没有数据，筛选功能仍可正常工作，但不会显示结果
2. 区域筛选按钮数量取决于数据库中的区域数量
3. 难度筛选目前只支持预定义的三个级别

## 故障排查

### 搜索不跳转
- 检查 SearchBox 组件是否正确导入
- 检查浏览器控制台是否有错误

### 筛选不生效
- 检查数据库连接是否正常
- 检查是否有符合筛选条件的数据
- 检查浏览器控制台是否有错误

### 样式异常
- 检查 CSS 模块是否正确导入
- 检查是否有样式冲突
