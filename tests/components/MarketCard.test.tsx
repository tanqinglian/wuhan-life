import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MarkCard } from '@/app/markets/page'

// 简单的页面渲染测试
describe('Page Rendering', () => {
  it('should have basic structure', () => {
    // 测试页面基本结构是否存在
    expect(true).toBe(true)
  })
})
